<!DOCTYPE html>
<html>
<head>
  <title><%= @user.name %>の体温記録</title>
  <link rel="stylesheet" href="<%= asset_path 'styles.css' %>">
</head>
<body>
  <div class="container">
    <h1><%= @user.name %>の体温記録</h1>

    <h2>体温リスト</h2>
    <% if @temperatures.any? %>
      <table>
        <thead>
          <tr>
            <th>記録日時</th>
            <th>体温</th>
          </tr>
        </thead>
        <tbody>
          <% @temperatures.each do |temperature| %>
            <tr>
              <td><%= temperature.created_at.strftime('%Y年%m月%d日 %H:%M') %></td>
              <td><%= temperature.netu %> °C</td>
            </tr>
          <% end %>
        </tbody>
      </table>
    <% else %>
      <p>体温の記録がありません。</p>
    <% end %>
  </div>
</body>
</html>
